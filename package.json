{
  "name": "mongo-change-tracker",
  "version": "1.0.0",
  "description": "Realtime MongoDB change tracker: watches Change Streams, groups events by collection+documentId, shows method name + changed fields, and validates documents against JSON Schema templates.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongodb": "^6.9.0"
  }
}
